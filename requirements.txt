# ============================================================================
# AstroManager - Dependencies
# ============================================================================
# Install: pip install -r requirements.txt
# Note: AstroManager auto-installs missing dependencies on first launch.
# ============================================================================

# Core (required)
numpy>=1.20
astropy>=5.0
matplotlib>=3.5
pandas>=1.3
Pillow>=9.0
PyQt6>=6.2
tqdm>=4.60
requests>=2.25
reportlab>=3.6
xisf>=0.9

# Compression codecs (recommended - enables zstd/lz4 compression)
zstandard>=0.18
lz4>=4.0

# System monitoring (recommended - enables auto-tuning)
psutil>=5.9

# Configuration (recommended)
PyYAML>=6.0

# XML security (recommended - protects against XXE/billion laughs attacks)
defusedxml>=0.7

# SIMBAD target resolution (optional - enables astronomical catalog queries)
astroquery>=0.4

# Scientific computing (optional - enables advanced statistics)
scipy>=1.7
